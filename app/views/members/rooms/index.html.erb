<div class="container mt-3">
  <% if @rooms.exists? %>
  <div class="row mt-3">
    <table class="table table-hover table-inverse">
      <thead>
        <tr>
          <th>取引ステージ</th>
          <th>投稿者</th>
          <th>投稿詳細</th>
        　<th>取引開始日</th>
        </tr>
      </thead>
      <tbody>
        <% @rooms.each do |room| %>
        <%# binding.pry %>
        <tr>
          <td>
            <% if room.is_active == "in_transaction" %>
              <%= link_to "チャットをする", room_path(room),class:"btn btn-outline-dark" %>
            <% else room.is_active == "in_review" %>
              <%= link_to "評価をする", room_path(room), class:"btn btn-outline-danger" %>
            <% end %>
          </td>
          <td>
            <% if room.member_id == current_member.id %>
              自分
            <% else %>
              <%= link_to room.member.nickname, member_path(room.member) , class:"text-dark" %>
            <% end %>
          </td>
          <td><%= link_to room.title, request_path(room),class:"text-dark" %></td>
          <td><%= room.created_at.strftime('%Y/%m/%d') %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <% else %>
  <div class="row justify-content-center">
    <div class="col-5 mt-3">
      <h4>取引はありません</h4>
    </div>
  </div>
  <% end %>
  <div class="row  mt-3 mb-3 justify-content-center"> <%= paginate @rooms %></div>
</div>