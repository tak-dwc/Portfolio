<% if request.liked_by?(current_member) %>
  <%= link_to request.likes.count, request_like_path(request), method: :delete,remote: true %>
<% else %>
  <%= link_to request.likes.count, request_like_path(request), method: :post, remote: true %>
<% end %>